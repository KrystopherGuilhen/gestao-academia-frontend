export interface IToolbar {
    minimalista: boolean;
    btnCircular: boolean;
    btnSombreado?: boolean;
    tabelaCrud: boolean;
    apenasXlsx: boolean;
    podeCriar?: boolean; // Nova propriedade
    podeExcluir?: boolean; // Nova propriedade
}
